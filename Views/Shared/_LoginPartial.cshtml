@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

<div class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
    
    <a  class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage"><button class="login-btn profile-btn" id="profile-button"><img src="~/user.png" alt="user icon">Profile</button></a>
    <div id="profile-dropdown" class="hidden">
        <img src="" alt="">
        <a  class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">@User.Identity.Name</a>
        <form asp-area="Identity" asp-page="/Account/Logout"
            asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button type="submit" >Logout</button>
        </form>
    </div>
}
else
{
    
        <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Login"><button class="login-btn profile-btn"><img src="~/user.png" alt="user icon">Login</button></a>
    
}
</div>
